apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owasp-data-pvc
  namespace: security-tools
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
